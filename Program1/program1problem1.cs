/*
Implement the basic methods of the stack in procedural code. Set the stack up so that 
it can work with characters. Use the push and pop methods to load and unload the 
following strings from the stack, print out the results. 
a. “bob”
b. “aet nomel”
c. “snoino dna revil etah I”
d. “SELUR LOBOC”
*/

main(){

    string arr[] = [“bob”, “aet nomel”, “snoino dna revil etah I” , “SELUR LOBOC”];


    int j;
    for(j = 0;j<string.lenght;j++){
        printf(hello);
    }

}

/*
#include <stdio.h>

class char Stack{
    char s[20];
    int top;

    void init() {
        top = -1;
    }
    boolean isStackEmpty(){
        boolean answer;
        answer = false;
        if(top == -1){
            answer = true;
        }
        return answer;
    }

    void push(char c){
        top++;
        s[top] = c;
    }

    char pop(){
        char c;
        c = s[top];
        top--;
        return c;
    }
    void showStack(){
        int j;
        for (j=0;j<=top;j++){
            print(s[j]);
        }
    }
    void testThisMug(){
        int j; char c; charStack s;
        s, int();

        for(j=0;j<=n.lenght();j++){
            c=n.charAt(j);
            print(c);
            s.push(c);
        }
        s.showStackEmpty();

        while (s.isStackEmpty() == false){
            print(s.pop());
        }
    }
} // end class
*/